const base = '../api/sentinl';

export default {
  WATCHER_EDIT: {
    ALL: {
      COUNT: `${base}/watcher/editor/count/all`,
      AVERAGE: `${base}/watcher/editor/average/all`,
      SUM: `${base}/watcher/editor/sum/all`,
      MIN: `${base}/watcher/editor/min/all`,
      MAX: `${base}/watcher/editor/max/all`,
    },
  },
};
